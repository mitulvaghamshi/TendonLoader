# Tendon Loader App

```css
::before
```

- Review this [README.md](../server/README.md) on how to start the api server.

```css
::after
```

- Run this app as below:

```ps
# cwd: `tendon_loader`.
$ flutter run -d macos --dart-define-from-file=.env
```
