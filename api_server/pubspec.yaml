name: api_server
description: A server app for tendon_loader app.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  sqlite3: ^3.1.1
  uuid: ^4.5.2

dev_dependencies:
  extra_lints:
    git:
      url: https://github.com/mitulvaghamshi/extra_lints.git
  http: ^1.1.0
  test: ^1.24.0
