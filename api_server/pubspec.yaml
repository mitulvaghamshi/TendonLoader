name: api_server
description: A server app for tendon_loader app.
publish_to: none

version: 1.0.0

environment:
  sdk: ^3.13.0

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_static: ^1.1.3
  sqlite3: ^3.1.1
  uuid: ^4.5.2

dev_dependencies:
  http: ^1.1.0
  lints: ^6.1.0
  test: ^1.24.0
